# ARCHIVED

This repository has been merged into [spoint](https://github.com/AnEntrypoint/spoint) as an
in-repo npm workspace at `packages/mapspinner` (as of spoint commit `640603e8`, 2026-07-07).

The canonical source for mapspinner is now `spoint/packages/mapspinner`. spoint's
`node_modules/mapspinner` resolves there via the workspace link, so a `git clone` +
`npm install` of spoint gets a fully live, editable copy with no vendoring step.

Do not edit this checkout directly -- changes made here will not reach spoint and will
silently diverge from the canonical source. Open changes against `spoint/packages/mapspinner`
instead.

This repository is kept for history only.
